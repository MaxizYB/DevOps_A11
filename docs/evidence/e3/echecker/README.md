# EChecker E3 Evidence Boundary

[#7](https://github.com/MaxizYB/DevOps_A11/issues/7) 在此保存 C0/C1/C2 的真实 SHA、人工 Oracle、命令快照与实际观察摘要。建议使用 `commits.json`、`oracle.json`、`commands.md`、`observations.md` 和 `failures.md`。

证据必须能说明 `base_commit`、baseline commit 和当前 commit 的关系，以及为什么同一 `configuration_id` 对增量比较是必要的。
